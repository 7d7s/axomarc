[package]
name = "sovereign-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Sovereign core use cases and domain types (hexagonal architecture)."

[dependencies]
anyhow = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
async-trait = { workspace = true }
sqlx = { workspace = true }
sha2 = "0.10"
tokio = { workspace = true }
tokio-util = { workspace = true }

[dev-dependencies]
tempfile = "3"
# Integration tests for the migrate use case (V0.1.0 → V0.5
# master-key migration) need both adapters; they're dev-only
# so the sovereign-core library stays adapter-agnostic.
sovereign-storage-sqlite = { path = "../sovereign-storage-sqlite" }
sovereign-secrets-age = { path = "../sovereign-secrets-age" }
secrecy = { workspace = true }
age = { workspace = true }
